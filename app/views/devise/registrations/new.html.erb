<div class="space-y-6">
  <div class="text-center">
    <h2 class="auth-title text-3xl font-bold text-slate-900">
      Criar conta
    </h2>
    <p class="mt-2 text-sm text-slate-600">
      Junte-se à comunidade Forja e comece sua jornada
    </p>
  </div>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "space-y-5" }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div>
      <%= f.label :email, "E-mail", class: "block text-sm font-medium text-slate-700 mb-2" %>
      <%= f.email_field :email,
          autofocus: true,
          autocomplete: "email",
          class: "w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent transition-all outline-none",
          placeholder: "seu@email.com" %>
    </div>

    <div>
      <%= f.label :password, "Senha", class: "block text-sm font-medium text-slate-700 mb-2" %>
      <% if @minimum_password_length %>
        <p class="text-xs text-slate-500 mb-2">
          (Mínimo de <%= @minimum_password_length %> caracteres)
        </p>
      <% end %>
      <%= f.password_field :password,
          autocomplete: "new-password",
          class: "w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent transition-all outline-none",
          placeholder: "••••••••" %>
    </div>

    <div>
      <%= f.label :password_confirmation, "Confirme a senha", class: "block text-sm font-medium text-slate-700 mb-2" %>
      <%= f.password_field :password_confirmation,
          autocomplete: "new-password",
          class: "w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent transition-all outline-none",
          placeholder: "••••••••" %>
    </div>

    <div>
      <%= f.submit "Criar conta", class: "w-full px-4 py-3 bg-amber-600 text-white font-semibold rounded-lg hover:bg-amber-700 focus:ring-4 focus:ring-amber-300 transition-all cursor-pointer" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
